# Ghost CMS — server-side only (NOT prefixed with NEXT_PUBLIC_)
# Set these in your deployment platform's environment / secrets manager.
# NEVER commit real values to version control.

# The base URL of your Ghost instance (no trailing slash)
GHOST_API_URL=https://glamgirlshaven.com

# Ghost Content API key (Admin → Integrations → Add custom integration)
GHOST_CONTENT_API_KEY=1337fc59d0b12e61b33bacafc2

GHOST_ADMIN_API_KEY=686a4e8fe286f7604dc205fc:4eee2b8b26510f818be9a3bafcfa00c120cfb51a429a671b746fff980910d6e5

# Public site URL — safe to expose to the browser
NEXT_PUBLIC_SITE_URL=https://glamgirlshaven.com

# Allow self-signed certificates for Node.js fetch (useful in local dev with self-signed SSL)
# NODE_TLS_REJECT_UNAUTHORIZED=0
